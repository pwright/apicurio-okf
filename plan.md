# Apicurio OKF Plan

Create a small, reproducible OKF-style workspace where the curated Apicurio Registry repository is copied into `human/`, then used as the source for generated and reviewed agent-readable knowledge.

## Source

Populate `human/` from `Apicurio/apicurio-registry` main and record the commit in:

```text
human/apicurio-registry/_source.md
sources/apicurio-registry.md
```

## Initial outputs

Keep `maps/` empty until an `apicurio.bs` Blockscape map is created.
